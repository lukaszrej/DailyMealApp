import FormControl from '@material-ui/core/FormControl';
import styled from "styled-components";

export const UserDetailsEdit = styled(FormControl)`
	width: 100%;

	.buttons {
		display: flex;
		justify-content: center;
		margin: 0;
		padding: 8px;
	}

`;

export const UserDetailsButtons = styled.div`
	display: flex;
	justify-content: center;
	margin: 0;
	padding: 8px;
`;
